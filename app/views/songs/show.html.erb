<div class="container">
<p id="notice"><%= notice %></p>
<div>
  <div class="">
    <strong>Name:</strong>
    <%= @song.name %>
  </div>
    <div>  
  <audio controls="true">
    <source src=<%=  @song.audio.url %> type="audio/mpeg">
    Your browser does not support the audio element.
  </audio>
</div>
</div>

<%= link_to 'Edit', edit_song_path(@song), :class => 'btn btn-link' %> |
<%= link_to 'Back', singers_path , :class => 'btn btn-link'%>
</div>